# AGENTS.md

## Cursor Cloud specific instructions

This repository (`tooler`) is currently an empty/blank project with only a `README.md`.

- **No build system, package manager, or dependencies** are configured yet.
- **No services** need to be started.
- **No lint, test, or build commands** are available.

When the repository gains actual code and dependencies, this section should be updated with:
- How to install dependencies
- How to run lint/test/build
- How to start dev services
- Any non-obvious caveats discovered during setup
